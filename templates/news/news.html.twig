{% extends "base.html.twig" %}

{% block title %}
   - News Page
{% endblock %}

{% block stylesheets %}
    {{ parent() }}
{% endblock %}

{% block body %}
    <div class="flex flex-col items-center justify-center container">
    
        {% for new in news %}

            <article class="prose">

                <h2>
                    <a class="no-underline" href="{{ new.url }}" target="_blank">
                        {{ new.title }}
                    </a>
                </h2>
                <a href="{{ new.url }}" target="_blank">
                    <img src="{{ new.imageUrl }}" alt="{{ new.title }}"/>
                </a>
                <p>
                  {{ new.summary }}
                </p>
                <p>
                    <a type="button" href="{{ new.url }}" target="_blank" class="no-underline inline-block px-6 py-2.5 bg-blue-600 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:bg-blue-700 hover:shadow-lg focus:bg-blue-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-blue-800 active:shadow-lg transition duration-150 ease-in-out">Read more on {{ new.newsSite }}</a>
                </p>
                <pre>Published at {{ new.publishedAt|date("d/m/y h:i:s") }}</pre>
                
            </article>

            <pre>---------------------------------------------------------</pre>
            
        {% endfor %} 

        {# {% for new in news %}
            <div class="justify-center">
                <div class="rounded-lg shadow-lg bg-white max-w-sm">
                    <a href="{{ new.url }}" target="_blank">
                    <img class="rounded-t-lg" width="182px" height="182px" src="{{ new.imageUrl }}" alt="{{ new.title }}"/>
                    </a>
                    <div class="p-6">
                    <h5 class="text-gray-900 text-xl font-medium mb-2">{{ new.title }}</h5>
                    <p class="text-gray-700 text-base mb-4">
                        {{ new.summary }}
                    </p>
                     <a type="button" href="{{ new.url }}" target="_blank" class=" inline-block px-6 py-2.5 bg-blue-600 text-white font-medium text-xs leading-tight uppercase rounded shadow-md hover:bg-blue-700 hover:shadow-lg focus:bg-blue-700 focus:shadow-lg focus:outline-none focus:ring-0 active:bg-blue-800 active:shadow-lg transition duration-150 ease-in-out">Read more on {{ new.newsSite }}</a>
                    </div>
                </div>
            </div>
        {% endfor %} #}
        
    </div>
{% endblock %}